name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      # Since Gradle 8 the root build no longer runs buildSrc tests, so the release
      # pipeline's wire-format tests must be invoked explicitly to gate CI.
      - name: Build everything
        run: ./gradlew :buildSrc:test build --no-daemon --stacktrace

      - name: Upload fat JARs
        uses: actions/upload-artifact@v4
        with:
          name: community-scripts-jars
          path: |
            **/build/libs/*.jar
          if-no-files-found: warn
          retention-days: 7
